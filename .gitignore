# dependencies
node_modules

# build output
dist

# environment / secrets — never commit
.env
.env.local
.env.*.local
supabase/.env*

# logs & OS noise
npm-debug.log*
*.log
.DS_Store
node_modules/
.next/
dist/
build/

# Wrangler local state
.wrangler

# Playwright E2E output
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.playwright-cli/

# Worker build artifact (tsc output inside cloudflare/workers)
cloudflare/workers/*.js
cloudflare/workers/*.d.ts

# agent / IDE tooling state (never commit)
.mimosa/
.cursor/
.claude/
.claude/scheduled_tasks.lock
