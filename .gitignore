# dependencies
node_modules

# build output
dist

# environment / secrets — never commit
.env
.env.local
.env.*.local
supabase/.env*

# logs & OS noise
npm-debug.log*
*.log
.DS_Store
node_modules/
.next/
dist/
build/

# Wrangler local state
.wrangler

# Playwright E2E output
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Worker build artifact (tsc output inside cloudflare/workers)
cloudflare/workers/*.js
cloudflare/workers/*.d.ts

# agent tooling state (never commit)
.mimosa/
.claude/scheduled_tasks.lock
