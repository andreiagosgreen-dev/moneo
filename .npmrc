# Workaround for an npm 10.9 arborist crash (#loadPeerSet null node)
# triggered by vitest 4.x's large optional-peer graph in this environment.
# legacy-peer-deps skips peer-set resolution; direct deps are still installed
# exactly as declared in package.json.
legacy-peer-deps=true
